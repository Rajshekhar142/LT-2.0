/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    // Note: It is now "@tailwindcss/postcss", not "tailwindcss"
    "@tailwindcss/postcss": {}, 
  },
};

export default config;